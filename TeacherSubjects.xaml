<Window x:Class="Learning_Center.TeacherSubjects"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Learning_Center"
        mc:Ignorable="d"
        AllowsTransparency="True"
        Title="TeacherSubjects" Height="450" Width="600" Background="Transparent" WindowStyle="None" WindowStartupLocation="CenterScreen">
    <Border CornerRadius="5" Background="{DynamicResource DarkBackground}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="20*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="20*"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Grid.Column="1">
                <TextBlock Text="Create Test" Margin="0 10" FontSize="18" Foreground="{DynamicResource LightText}"/>
            </StackPanel>
            <Ellipse Grid.Row="0" Grid.Column="2" Fill="Red" Width="15" Height="15" Margin="0 0 5 0" MouseLeftButtonUp="ButtonCloseTeacherSubjects"/>
        </Grid>
    </Border>
</Window>
